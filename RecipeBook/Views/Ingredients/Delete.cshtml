@model RecipeBook.Models.Ingredient

@{
    ViewData["Title"] = "Изтриване на съставка";
}

<h1 class="mb-4 text-danger">Изтрий съставка</h1>

<div class="alert alert-warning">
    Сигурни ли сте, че искате да изтриете следната съставка?
</div>

<div class="mb-4">
    <dl class="row mt-3">
        <dt class="col-sm-3">Име:</dt>
        <dd class="col-sm-9">@Model.Name</dd>

        <dt class="col-sm-3">Категория:</dt>
        <dd class="col-sm-9">@Model.IngredientCategory?.Name</dd>
    </dl>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-danger">Изтрий</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Назад</a>
    </div>
</form>
