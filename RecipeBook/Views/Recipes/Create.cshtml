@model RecipeBook.Views.Recipes.ViewModels.RecipeViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Recipe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Recipe.Title" class="control-label"></label>
                <input asp-for="Recipe.Title" class="form-control" />
                <span asp-validation-for="Recipe.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Recipe.Description" class="control-label"></label>
                <input asp-for="Recipe.Description" class="form-control" />
                <span asp-validation-for="Recipe.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Recipe.Instructions" class="control-label"></label>
                <input asp-for="Recipe.Instructions" class="form-control" />
                <span asp-validation-for="Recipe.Instructions" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Recipe.Servings" class="control-label"></label>
                <input asp-for="Recipe.Servings" class="form-control" />
                <span asp-validation-for="Recipe.Servings" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Recipe.CookingTime" class="control-label"></label>
                <input asp-for="Recipe.CookingTime" class="form-control" />
                <span asp-validation-for="Recipe.CookingTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Recipe.UserId" class="control-label"></label>
                <select asp-for="Recipe.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="imageFile">Upload Image</label>
                <!-- Ensure this name matches the parameter name in your controller -->
                <input type="file" asp-for="imageFile" class="form-control" accept="image/*" />
            </div>
            <div id="ingredient-list">
                @foreach (var ingredient in Model.Ingredients)
                {
                    <div class="form-group">
                        <label>@ingredient.Name</label>
                        <input type="hidden" name="Ingredients[@Model.Ingredients.IndexOf(ingredient)].IngredientId" value="@ingredient.IngredientId" />
                        <input type="text" name="Ingredients[@Model.Ingredients.IndexOf(ingredient)].QuantityNeeded" class="form-control" placeholder="Enter quantity" />
                    </div>
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
